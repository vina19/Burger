<div class="mt-4">
	<li>
		{{burger_name}} &nbsp;
		
		{{#unless devoured}}
			<button type="button" class="btn btn-primary change-devour" data-id="{{id}}" data-newsleep="{{sleep}}">
				Devour it!!
			</button> &nbsp;
		{{/unless}} 
		
		<button type="button" class="btn btn-primary delete-btn" data-id="{{this.id}}">Delete</button>
	</li>
</div>
